<br>
<div class="container">
	<% @posts.each do |p| %>
		<div class="row">
			<div class="col-md-12">
			<h3><%= p.title %></h3>
			<p><em><%= p.created_at.strftime('%m/%d/%Y') %></em></p>
			<article>
				<%= p.text.html_safe %>
			</article>
			</div>
		</div>
	<% end %>
</div>
